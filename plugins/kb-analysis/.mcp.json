{
  "mcpServers": {
    "gdrive": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-gdrive"],
      "env": {
        "GOOGLE_APPLICATION_CREDENTIALS": "${GOOGLE_APPLICATION_CREDENTIALS}"
      },
      "description": "Google Drive access for Docs, Slides, Sheets. Requires GOOGLE_APPLICATION_CREDENTIALS in kb/.kb-config"
    },
    "figma": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-figma"],
      "env": {
        "FIGMA_ACCESS_TOKEN": "${FIGMA_ACCESS_TOKEN}"
      },
      "description": "Figma design file access. Requires FIGMA_ACCESS_TOKEN in kb/.kb-config"
    },
    "notion": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-notion"],
      "env": {
        "NOTION_API_KEY": "${NOTION_API_KEY}"
      },
      "description": "Notion workspace access. Requires NOTION_API_KEY in kb/.kb-config"
    },
    "confluence": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-confluence"],
      "env": {
        "CONFLUENCE_URL": "${CONFLUENCE_URL}",
        "CONFLUENCE_TOKEN": "${CONFLUENCE_TOKEN}"
      },
      "description": "Confluence wiki access. Requires CONFLUENCE_URL and CONFLUENCE_TOKEN in kb/.kb-config"
    }
  }
}
